import React from 'react';
//Toda vez q tiver um html(tag) tem q importar o react

import ReactDOM from 'react-dom';
//React DOM comunicação

import App from './App';


ReactDOM.render(<App />, document.getElementById('root'));
//Renderizando o app no root

//JSX (JavaScript + HTML)